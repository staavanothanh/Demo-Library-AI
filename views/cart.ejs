<%- include("partials/header") %>
  <section class="page-heading">
    <p class="eyebrow">YOUR CART</p>
    <h1>Ready when you are.</h1>
    <p>A short list of books waiting for you.</p>
  </section>
  <p id="cart-loading" class="htmx-indicator muted" role="status" aria-live="polite">Updating your cart…</p>
  <%- include("partials/cart-content", { items, total, csrfToken, cartCount: cartCount || 0, fragment: false }) %>
  <%- include("partials/footer") %>
